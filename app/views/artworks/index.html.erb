<p id="notice"><%= notice %></p>

<h1>Artworks</h1>

<table>
  <thead>
    <tr>
      <th>Art title</th>
      <th>Art description</th>
      <th>Art price</th>
      <th>Available</th>
      <th>Orderitem</th>
      <th>Artist</th>
      <th>Style</th>
      <th>Artmedium</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @artworks.each do |artwork| %>
      <tr>
        <td><%= artwork.art_title %></td>
        <td><%= artwork.art_description %></td>
        <td><%= artwork.art_price %></td>
        <td><%= artwork.available %></td>
        <td><%= artwork.orderitem_id %></td>
        <td><%= artwork.artist_id %></td>
        <td><%= artwork.style_id %></td>
        <td><%= artwork.artmedium_id %></td>
        <td><%= link_to 'Show', artwork %></td>
        <td><%= link_to 'Edit', edit_artwork_path(artwork) %></td>
        <td><%= link_to 'Destroy', artwork, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Artwork', new_artwork_path %>
